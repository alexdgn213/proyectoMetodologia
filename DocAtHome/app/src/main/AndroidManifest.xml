<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="abmprogrammieren.com.docathome">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActividadPrincipal"
            android:label=""
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Perfil"
            android:label="Perfil" />
        <activity
            android:name=".PantallaStreaming"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginDoctor"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PrincipalDoctor"
            android:label="@string/title_activity_principal_doctor"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>